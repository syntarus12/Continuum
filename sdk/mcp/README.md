# Continuum MCP

MCP server exposing least-privilege search and write tools for Continuum memory.
It reads `SYNTARUS_API_KEY` from the process environment. Do not put API keys
in an MCP configuration file.
