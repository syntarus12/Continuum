# Minimal Community Edition example dependency.
# Install the local SDK with: python -m pip install -e ./sdk
# Frameworks are intentionally optional; install only what your agent uses.

# langchain-core>=0.3
# langgraph>=0.2
# langchain-openai>=0.2
# crewai>=0.80
